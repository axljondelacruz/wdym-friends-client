import Container from '@mui/material/Container';
import Router from './Router';

const App = () => {
  return (
    <Container maxWidth="sm">
      <Router />
    </Container>
  );
};

export default App;
